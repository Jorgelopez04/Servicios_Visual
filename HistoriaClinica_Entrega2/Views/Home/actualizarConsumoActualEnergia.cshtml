@{
    ViewBag.Title = "actualizarConsumoActualEnergia";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <title>Actualizar consumo actual de energia</title>
    <link rel="stylesheet" href="~/Content/PersonaVista.css" />
</head>
<body>
    <div>
        <h3>Actualizar consumo actual de energia</h3>
        @if (!string.IsNullOrEmpty(ViewBag.Notificacion))
        {
            <div class="alert alert-danger">@ViewBag.Notificacion</div>
        }
        @using (Html.BeginForm("verificarParaActualizarConsumoActualEnergia", "Home", FormMethod.Post))
        {
            <table>
                <h4>Para actualizar el consumo actual de energia el cliente debe ingresar su identificación</h4>
                <tr>
                    <td><label>Identificación:                       </label></td>
                    <td><input type="text" name="idConsumoActualEnergia" pattern="[0-9]+" oninput="this.value=this.value.replace(/[^0-9]/g,'');" value=" " /></td>
                </tr>
                <tr>
                    <td><br /></td>
                    <td></td>
                </tr>

                <tr>
                    <td><br /></td>
                    <td></td>
                </tr>
                <tr>
                    <td><input type="submit" name="enviar" value="Enviar" /></td>
                </tr>
            </table>
        }
    </div>
</body>
</html>


